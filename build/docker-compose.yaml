version: "3.9"
services:
    app:
        container_name: api-test
        build: 
            context: ../
            dockerfile: build/Dockerfile
        image: go-api-test:latest
        ports: 
            - "8080:8080"